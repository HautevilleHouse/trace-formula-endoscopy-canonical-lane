# Identification Bridge

This note records the determining-class bridge for `trace-formula endoscopy`. It fixes the endpoint representative, tracks `eps_coh`, and closes the final strict margin `M_TFE` in strict-coherence mode.
